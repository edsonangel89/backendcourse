<nav class="navbar navbar-expand container-fluid shadow position-fixed z-1">
    <div class="logo-container">
        <img class="brand-logo" src="http://localhost:8080/assets/logo__svg.svg"/>
    </div>
    <div class="form-container">
        <form class="form-close" action="http://localhost:8080/api/sessions/logout" method="get">
            <!--<a class="button-account" role="button">account_circle</a>-->
            <h6 class="mx-3 h6-nav">Hola <strong>{{name}}</strong></h6>
            <span role="button" class="material-symbols-outlined button-account">account_circle</span>
            <!--<button class="btn button-close" type="submit">Cerrar sesión</button>-->
        </form>
    </div>
</nav>
<div class="z-0 cards" >  
    {{#each productsList}}
        <div class="card m-3" style="width: auto;">
            <img src='http://localhost:8080/assets/images/{{_doc.thumbnail}}.jpg' class="card-img-top" alt="Item image">
            <div class="card-body">
                <div class="row">
                    <h4 class="card-title col">{{_doc.title}}</h4>
                </div>
                 <div class="row border" style="height: 3.5rem;">
                    <p class="card-text col border h-100">{{_doc.description}}</p>
                </div>
                <div class="row border" style="height: 3.5rem;">
                    <!--<h6 class="col border h-100 d-flex align-items-center justify-content-center">Code</h6>-->
                    <p class="col border h-100">{{_doc.price}}</p>
                </div>
            </div>
        </div>
    {{/each}}
</div>