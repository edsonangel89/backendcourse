import mongoose from "mongoose"
import { } from '../src/controllers/sessions.controller.js'
import Assert from 'assert'

mongoose.connect()

const assert = Assert.strict

describe('Test de sesiones', () => {
    before()
    it('', () => {

    })
    it('', () => {

    })
    it('', () => {

    })
    beforeEach()
})