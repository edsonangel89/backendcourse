import mongoose from "mongoose"
import { getCartById, addProduct, deleteProduct, deleteCart} from '../src/controllers/carts.controller.js'
import Assert from 'assert'

mongoose.connect()

const assert = Assert.strict

describe('Test de carritos', () => {
    before()
    it('', () => {

    })
    it('', () => {

    })
    it('', () => {

    })
    beforeEach()
})